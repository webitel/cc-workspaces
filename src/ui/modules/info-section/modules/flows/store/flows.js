const state = {
  flows: [],
};

const getters = {};

const actions = {};

const mutations = {};

// empty store for future usage

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
  modules: {},
};
