const ChatTransferDestination = Object.freeze({
                                                USER: 'user',
                                                CHATPLAN: 'chatplan',
                                              });

export default ChatTransferDestination;
